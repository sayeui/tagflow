.DS_Store

# Rust 构建产物
tagflow-core/target

# Node.js 依赖和构建产物
tagflow-ui/node_modules
tagflow-ui/dist

# SQLite 数据库文件（运行时生成）
*.db
*.db-shm
*.db-wal

# Docker 镜像导出产物（docker save 输出，用于传输到 NAS）
*.tar

# Claude Code 本地个人配置
.claude/settings.local.json
.claude/worktrees/
target
